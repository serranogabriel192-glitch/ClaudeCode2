# Server
PORT=3000

# Admin password (required to access admin dashboard)
ADMIN_PASSWORD=Letmein2026

# Email notifications (optional — leave blank to disable)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_FROM=reception@flyerdefense.com
